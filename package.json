{
  "name": "playwright-course-package",
  "version": "1.0.0",
  "description": "Playwright course",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:chrome": "npx playwright test --config=playwright.config.js --project=Chrome"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.28.0"
  }
}
